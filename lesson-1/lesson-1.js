// Task #1

var celsiumTemp = 25; // Задали температуру в Цельсиях в 25 градусов
var faringeitTemp = (9/5) * celsiumTemp + 32; // Перевели температуру по формуле в Фаренгейты

alert('Temperature in Farengeit is ' + faringeitTemp);

// Task #2

var admin;
var name;

name = 'Василий';
admin = name;

alert(admin);

// Task #3

// Выражение 1000 + "108" будет равно 1000108, т.к. операция сложения будет выполняться со строчными элементами

// Task #4